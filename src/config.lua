Config = {}

Config.Locale = 'en'

-- If true Player needs to have the tablet item to use it / If false everyone cann call it with a Hotkey
Config.EnableItem = false

-- Key to open the Tablet if "EnableItem" is set to false
Config.OpenKey = 